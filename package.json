{
  "name": "browser-automation-toolkit",
  "version": "2.3.0",
  "description": "Browser automation via Chrome extension and HTTP API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "docker compose -f docker/docker-compose.yml run --rm test",
    "migrate": "node src/database/migrate.js",
    "client": "python3 client/server.py"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "keywords": ["browser", "automation", "chrome", "extension"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
